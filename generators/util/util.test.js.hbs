import { describe, expect, test } from "vitest";

import { {{camelCase name}} } from "./";

describe("{{camelCase name}}", () => {
  test("executes successfully", () => {
    const response = {{camelCase name}}("foo");

    expect(response).toBe("foo");
  });
});
