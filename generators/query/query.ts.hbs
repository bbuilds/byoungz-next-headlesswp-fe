import { fetchGraphQL } from "@/src/lib/api";
import { Page } from "@/src/lib/types";

export async function {{camelCase name}}(slug: string) {
  const query = `
    //insert query here
  `;

  const variables = {
    slug: slug,
  };

  const response = await fetchGraphQL(query, variables);

  return response.data.page as Page;
}
